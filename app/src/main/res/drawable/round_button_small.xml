<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- PRESSED STATE -->
    <item android:state_pressed="true">
        <shape xmlns:android="http://schemas.android.com/apk/res/android" android:innerRadius="0dp" android:shape="ring" android:thickness="50dp" android:useLevel="false">
            <solid android:color="@color/Primary" />
            <stroke android:width="2dp" android:color="@color/Primary_Accent" />
        </shape>

        <!-- FOCUSED STATE -->
    </item>
    <item android:state_focused="true">
        <shape xmlns:android="http://schemas.android.com/apk/res/android" android:innerRadius="0dp" android:shape="ring" android:thickness="50dp" android:useLevel="false">
            <solid android:color="@color/Primary" />
        </shape>
    </item>

    <!-- NORMAL STATE -->
    <item>
        <shape xmlns:android="http://schemas.android.com/apk/res/android" android:innerRadius="0dp" android:shape="ring" android:thickness="50dp" android:useLevel="false">
            <stroke android:width="2dp" android:color="@color/Primary_Accent" />
        </shape>
    </item>

</selector>